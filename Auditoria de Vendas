# Váriavel global
limite_segurança = 10000

def analisar_vendas(v1, v2, v3):
  global limite_segurança # Alteração do limite
  # Média
  media = (v1 + v2 + v3) / 3
  print(f"Média das vndas: R$ {media:.2f}")
  # Limite
  if media > limite_segurança:
   print("Sistema em quarentena!")
  # Verificação de discrepancia
  vendas = [v1, v2, v3]
  for venda in vendas:
   if venda > 5 * media:
    print("Revisão manual")
    break
  # Ajuste no limite
  resposta = input ("Alguma venda alta é legitima? (s/n):").lower
  if resposta == "s":
   limite_segurança = novo_limite
   print(f"Novo limite de segurança: R$ {limite_segurança:.2f}")
   # Exibição dos tipos
  print("tipos de dados")
  print(f"v1: {type(v1)}")
  print(f"v2: {type(v2)}")
  print(f"v3: {type(v3)}")
  print(f"media: {type(media)}")
  print(f"limite_segurança: {type(limite_segurança)}")
#entrada de dados
print("Sistema de Auditoria de Vendas")
venda1 = float(input("Digite o valor da primeira venda: R$"))
venda2 = float(input("Digite o valor da segunda venda: R$"))
venda3 = float(input("Digite o valor da terceira venda: R$"))
# Função
analisar_vendas(venda1, venda2, venda3)
